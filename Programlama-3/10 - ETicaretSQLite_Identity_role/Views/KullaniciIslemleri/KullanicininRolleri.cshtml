@model List<string>
@{
    ViewData["Title"] = "KullanicininRolleri";
}
<div class="jumbotron mt-5" >
    <h1 class="text-primary"> <span class="text-danger">@ViewBag.KullaniciAdi</span>Kullanıcısının Rolleri</h1>
    <table class="table table-hover">
        <thead>
            <th>Rol</th>
        </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item</td>
            </tr>
        }
    </table>
</div>
<a asp-action="Index" class="btn btn-primary mt-5">Geri</a>